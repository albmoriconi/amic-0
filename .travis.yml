language: minimal

install:
  - sudo add-apt-repository ppa:pgavin/ghdl -y
  - sudo apt-get update
  - sudo apt-get install ghdl cmake make -y

script:
  - mkdir build && cd build
  - cmake ..
  - make check
